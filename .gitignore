__pycache__/
*.py[cod]

# Runtime event store; EventStore creates this next to store.py by default.
*.db
smartbin.db

# Frames written on every trigger. Useful on the board, not in git.
python/captures/

# App Lab sketch build artifacts (~490 MB on the board).
.cache/
